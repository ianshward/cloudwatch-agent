A simple node.js daemon which submits AWS CloudWatch metrics.  Presently very limited in scope.

Usage:

* write some checks in /checks (see examples there)
* run as daemon or submit metrics one-off on cron.
